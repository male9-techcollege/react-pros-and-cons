/* Source: react-modal-codealong-med-kasper */
import backdropstyle from "./backdrop.module.scss";

/* It would also have been possible to use the pseudo-element backdrop, 
but the assignment required the use of a separate HTML element. */
export function BackdropElementByMariePierreLessard() {
    return (
        <div className={backdropstyle.backdropByMariePierreLessard}></div>
    );
};
